const workspaceRouter = require('./workspace')
const userViewRouter = require('./userView')
const userVisRouter = require('./userVisualisation')
const locationRouter = require('./location')
const measurementRouter = require('./measurement')

module.exports = {
    workspaceRouter,
    userViewRouter,
    userVisRouter,
    locationRouter,
    measurementRouter
}